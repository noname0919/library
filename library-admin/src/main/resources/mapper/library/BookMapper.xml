<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.BookMapper">
    
    <resultMap type="Book" id="BookResult">
        <result property="id"    column="id"    />
        <result property="isbn"    column="isbn"    />
        <result property="bookName"    column="book_name"    />
        <result property="authorName"    column="author_name"    />
        <result property="publisherName"    column="publisher_name"    />
        <result property="categoryId"    column="category_id"    />
        <result property="publishDate"    column="publish_date"    />
        <result property="price"    column="price"    />
        <result property="image"    column="image"    />
        <result property="totalQuantity"    column="total_quantity"    />
        <result property="availableQuantity"    column="available_quantity"    />
        <result property="borrowedQuantity"    column="borrowed_quantity"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectBookVo">
        select id, isbn, book_name, author_name, publisher_name, category_id, publish_date, price, image, total_quantity, available_quantity, borrowed_quantity, status, create_time, update_time from book
    </sql>

    <select id="selectBookList" parameterType="Book" resultMap="BookResult">
        <include refid="selectBookVo"/>
        <where>
            <if test="isbn != null  and isbn != ''"> and isbn = #{isbn}</if>
            <if test="bookName != null  and bookName != ''"> and book_name like concat('%', #{bookName}, '%')</if>
            <if test="categoryId != null "> and category_id = #{categoryId}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
        </where>
    </select>


    <select id="selectBookById" parameterType="Long" resultMap="BookResult">
        <include refid="selectBookVo"/>
        where id = #{id}
    </select>
    <select id="checkAddExist" resultType="java.lang.Boolean" parameterType="java.lang.String">
        select count(1) from book where isbn = #{isbn}
    </select>
    <select id="checkUpdateExist" resultType="java.lang.Boolean">
        select count(1) from book where isbn = #{isbn} and id != #{id}
    </select>
    <select id="selectBookListByIds" resultType="com.library.domain.Book" parameterType="long[]">
        <include refid="selectBookVo"/>
        WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>


    <insert id="insertBook" parameterType="Book" useGeneratedKeys="true" keyProperty="id">
        insert into book
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="isbn != null and isbn != ''">isbn,</if>
            <if test="bookName != null and bookName != ''">book_name,</if>
            <if test="authorName != null and authorName != ''">author_name,</if>
            <if test="publisherName != null and publisherName != ''">publisher_name,</if>
            <if test="categoryId != null">category_id,</if>
            <if test="publishDate != null">publish_date,</if>
            <if test="price != null">price,</if>
            <if test="image != null and image != ''">image,</if>
            <if test="totalQuantity != null">total_quantity,</if>
            <if test="availableQuantity != null">available_quantity,</if>
            <if test="borrowedQuantity != null">borrowed_quantity,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="isbn != null and isbn != ''">#{isbn},</if>
            <if test="bookName != null and bookName != ''">#{bookName},</if>
            <if test="authorName != null and authorName != ''">#{authorName},</if>
            <if test="publisherName != null and publisherName != ''">#{publisherName},</if>
            <if test="categoryId != null">#{categoryId},</if>
            <if test="publishDate != null">#{publishDate},</if>
            <if test="price != null">#{price},</if>
            <if test="image != null and image != ''">#{image},</if>
            <if test="totalQuantity != null">#{totalQuantity},</if>
            <if test="availableQuantity != null">#{availableQuantity},</if>
            <if test="borrowedQuantity != null">#{borrowedQuantity},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateBook" parameterType="Book">
        update book
        <trim prefix="SET" suffixOverrides=",">
            <if test="isbn != null and isbn != ''">isbn = #{isbn},</if>
            <if test="bookName != null and bookName != ''">book_name = #{bookName},</if>
            <if test="authorName != null and authorName != ''">author_name = #{authorName},</if>
            <if test="publisherName != null and publisherName != ''">publisher_name = #{publisherName},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="publishDate != null">publish_date = #{publishDate},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="totalQuantity != null">total_quantity = #{totalQuantity},</if>
            <if test="availableQuantity != null">available_quantity = #{availableQuantity},</if>
            <if test="borrowedQuantity != null">borrowed_quantity = #{borrowedQuantity},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteBookById" parameterType="Long">
        delete from book where id = #{id}
    </delete>

    <delete id="deleteBookByIds" parameterType="String">
        delete from book where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>